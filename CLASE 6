#include<stdio.h>
int main() 
{
	int nota;
	printf("Ingrese la nota final:");
	scanf("%d",&nota);
	if(nota>=70 && nota<=100) 
	{
		printf("Aprobado\n");
	}
	if (nota>=0 && nota<=100) 
	{
		printf("No Aprobado\n");
	}
	getch ();
	return 0;
}
