#include <stdio.h>
int main() 
{
    int numero, suma = 0;  
    printf("Ingrese un número entero positivo: ");
    scanf("%d", &numero);   
    for (; numero > 0; suma += numero % 10, numero /= 10);    
    printf("La suma de los dígitos es: %d\n", suma);
    getch ();
    return 0;
}
