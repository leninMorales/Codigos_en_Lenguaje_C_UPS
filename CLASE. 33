#include <stdio.h>
int main() 
{
    int numero, i;
    printf("Ingrese un número : ");
    scanf("%d", &numero);
    printf("Tabla de multiplicar de %d:\n", numero);
    for (i = 1; i <= 10; i++) 
	{
        printf("%d x %d = %d\n", numero, i, numero * i);
    }
    getch ();
    return 0;
}
