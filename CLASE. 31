#include <stdio.h>
int main()
{
    int N, i,suma = 0;
    printf("Ingrese el valor de N: ");
    scanf("%d", &N);
    for (i = 1; i <= N; i++) 
	{
        suma += i * i;
    }
    printf("La suma de los cuadrados de los %d primeros números es: %d\n", N, suma);
    getch ();
    return 0;
}
